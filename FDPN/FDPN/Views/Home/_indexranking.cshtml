@model List<FDPN.Models.RESULTS>

@{
    string prueba;
    if (Model[0].COURSE == "S")
    {
        prueba = @Model[0].DISTANCE + " " + @Model[0].STROKE + " - " + @Model[0].Athlete1.Sex + " Piscina larga";
    }
    else
    {
        prueba = @Model[0].DISTANCE + " " + @Model[0].STROKE + " - " + @Model[0].Athlete1.Sex + " Piscina larga";
    }

}

@*<div class="contenedor">
    <div class="card card-profile card-plain">
        <h2 class="title">Top 10</h2>

        <div class="card-body ">
            <h4 class="card-title">@prueba  </h4>
            <table class="table ">
                <thead>
                    <tr>
                        <th>#</th>
                        <th>Nombre</th>
                        <th>Tiempo</th>
                        <th>Fecha</th>
                    </tr>
                </thead>
                <tbody>
                    @for (int i = 0; i < Model.Count; i++)
                    {
                        <tr>
                            <td>@(i+1)</td>
                            <td><span>@Model[i].Athlete1.First.ToUpper()</span><span>  @Model[i].Athlete1.Last.ToUpper()</span> </td>
                            <td>
                                @Model[i].SCORE
                            </td>
                            <td>@Model[i].MEET1.Start.ToString("dd/MMM/yyyy")</td>
                        </tr>

                    }
                </tbody>
            </table>

        </div>

    </div>
</div>*@


<div class="center">
    <h2 class="title center">Top 10</h2>
    <h4 class="title center">@prueba  </h4>
    <table class="table info-tabla tablacenter">
        <thead>
            <tr>
                <th>#</th>
                <th>Nombre</th>
                <th>Tiempo</th>
                <th>Fecha</th>
            </tr>
        </thead>
        <tbody>
            @for (int i = 0; i < Model.Count; i++)
            {
                <tr>
                    <td>@(i+1)</td>
                    <td><span>@Model[i].Athlete1.First.ToUpper()</span><span>  @Model[i].Athlete1.Last.ToUpper()</span> </td>
                    <td>
                        @Model[i].SCORE
                    </td>
                    <td>@Model[i].MEET1.Start.ToString("dd/MMM/yyyy")</td>
                </tr>

            }
        </tbody>
    </table>
</div>

