@model List<FDPN.Models.Vivo>

@{
    string path = Url.Content("/Resultado");
}

@if (Model.Count() > 0)
{
    @*<div class="Modal">
        <label class="modal__bg" for="modal-1"></label>*@
        <div >
            @*<label class="modal__close" for="modal-1"></label>*@
            @*<center>
                <div class="pure-g cardsubtitulo-blanca">
                    <div class="pure-u-1 pure-u-md-1" style="text-align:center;"><p class="formatted__hugephoto__title TextoBannerDelgado">NOTICIAS</p></div>
                </div>
                <div class="pure-g cardsubtitulo-blanca">
                    @Html.ActionLink("Ver todos los resultados", "Envivo", "resultados", null, new { @class = "btn btn--negative btn--sml u-rounded" })
                    <div class="espaciado"></div>
                </div>
            </center>*@
            <table class="tablaNormal">
                <thead>
                    <tr>
                        <th>#</th>
                        <th>Nombre</th>
                        <th>Tiempo</th>
                    </tr>
                </thead>
                <tbody>
                    @foreach (var item in Model)
                    {
                    <tr>

                        <td>
                            @item.Nombre
                        </td>
                        <td>
                            @item.Fecha
                        </td>
                        <td>
                            <a href=@Url.Content(path + item.Directorio)>Ver resultados</a>
                        </td>
                    </tr>
                    }

                </tbody>
            </table>

        </div>
    @*</div>*@
}
